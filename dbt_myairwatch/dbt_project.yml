name: myairwatch
version: "1.0.0"
config-version: 2

profile: myairwatch

model-paths: ["models"]

models:
  myairwatch:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
